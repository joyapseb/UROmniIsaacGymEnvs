# See schema in factory_schema_config_base.py for descriptions of parameters.

defaults:
  - _self_
  - /factory_schema_config_base

sim:
  add_damping: True
  disable_contact_processing: False
    
env:
  env_spacing: 1.5
  ur5e_depth: 0.484
  table_height: 0.4 #0.33 unscrew / #0.30 screw
  ur5e_friction: 1.0
  table_friction: 0.3
  nut_offset : 0.002

